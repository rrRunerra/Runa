import React from "react";
import CalendarClientPage from "./CalendarClient";

export const metadata = {
  title: "Aquila > Calendar",
  description: "Airing and Release Calendar",
};

export default function AquilaCalendarPage(): React.JSX.Element {
  return <CalendarClientPage />;
}
